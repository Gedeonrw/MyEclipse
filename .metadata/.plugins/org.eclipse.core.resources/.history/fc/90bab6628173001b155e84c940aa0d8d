package lambda.one;

import java.util.stream.Stream;

public class RandonWithGenerate {

	public static void main(String[] args) {
		Stream<Double> st = Stream.generate(Math::random).limit(100);

	}

}
