package flattenArray;

import java.util.stream.Stream;

public class Test {

	public static void main(String[] args) {
		String[] str = {"James","jimmy","jane"};
		Stream<String> strm = Stream.of(str);
		strm.map(e -> e.toUpperCase()).forEach(System.out::println);

	}

}
